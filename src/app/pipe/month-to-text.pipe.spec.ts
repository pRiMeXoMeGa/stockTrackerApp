import { MonthToTextPipe } from './month-to-text.pipe';

describe('MonthToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
